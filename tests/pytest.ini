[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    -q
markers =
    slow: marks tests that need the real CSV (deselect with -m "not slow")
filterwarnings =
    ignore::UserWarning
    ignore::FutureWarning
    ignore::DeprecationWarning
